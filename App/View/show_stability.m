function show_stability(UIAxes, x, y)
    % Wyœwietlenie portretów fazowych

    % plot(X(:,1),X(:,2),'r');
    % hold on;
    % plot(X(:,1),X(:,3),'b');
    % hold on;
    
    plot(UIAxes, x, y, 'b');

%     hold(UIAxes,'on')
%     plot(UIAxes, eigenX, eigenY, 'r*');
end

